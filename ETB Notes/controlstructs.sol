pragma solidity 0.6.0;

contract MyContract {
    function foo() external {
        // <= / == / => / < / >
        if(msg.sender == 100)
    }
}

contract Mycontract {
    bool boolValue;
    function foo () external {
        bool isOK = true;
        while(isOK) {
            //
            if() {
                isOk = false;
            }
        }
    }
}